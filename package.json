{
    "name": "resumeapply-monorepo",
    "private": true,
    "workspaces": [
        "frontend"
    ],
    "scripts": {
        "frontend:build": "npm run build --prefix frontend",
        "frontend:dev": "npm run dev --prefix frontend",
        "backend:dev": "cd backend && python main.py",
        "build": "cd frontend && npx opennextjs-cloudflare build",
        "deploy": "cd frontend && npx wrangler deploy"
    }
}